#!/usr/bin/env node

const http = require("http");

const app = require("../app");
const { PORT } = require("../utils/const");

const server = http.createServer(app);

server.listen(PORT, () => {
  console.log(`Liquid Electrs app listening on port ${PORT}`);
});
